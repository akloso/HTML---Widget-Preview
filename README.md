# HTML & Widget Preview

A lightweight static browser tool for previewing HTML, iframe, form, button, and JavaScript widget snippets.

## Files

- `index.html` — page structure
- `styles.css` — responsive styling
- `script.js` — input modes and preview behavior

## Deployment

Enable GitHub Pages from the repository settings using the `main` branch and the repository root.
